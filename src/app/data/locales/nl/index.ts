import { categories } from './categories';
import { ui } from './ui';

/** Interfacetekst in het Nederlands. Puzzeltekst (puzzles.ts) wordt apart geladen, met de snapshot. */
export default { ui, categories };
